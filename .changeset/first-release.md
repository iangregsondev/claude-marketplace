---
"iangregson": major
---

First release. The catalogue is live and lists no plugins yet. From this release on, the catalogue carries its own version in `metadata.version` of `.claude-plugin/marketplace.json`. It moves when an entry is added, removed or repointed, never when a catalogued plugin releases.
